services:
  edu-registry-monolith-app:
    command: >
      sh -c "uvicorn main:app
      --host 0.0.0.0
      --port $${APP_PORT:-8000}
      --reload"
    environment:
      APP_ENV: development
    volumes:
      - .:/app
    stdin_open: true
    tty: true
